/**
 *	Converts a string of hexadecimal digits (including 
 *	 0x and 0X) into its equivalent integer value.
 *	The allowable digots are 0 through 9, a through
 *	f, and A through F.
 */
#include <stdio.h>

main(){

}